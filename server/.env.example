# Server Configuration
HOST=0.0.0.0
PORT=3000
MONGODB_URI=your-mongodb-connection-string
JWT_SECRET=your-jwt-secret-key
NODE_ENV=development

# Client Configuration  
CLIENT_URL=http://localhost:5173

# Optional: Additional CORS origins (comma-separated)
# ADDITIONAL_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# Optional: Network host override (auto-detected if not provided)
# NETWORK_HOST=192.168.1.100

# Production Example:
# HOST=0.0.0.0
# PORT=8080
# MONGODB_URI=mongodb://your-production-db-connection
# JWT_SECRET=your-production-jwt-secret
# NODE_ENV=production
# CLIENT_URL=https://yourapp.com
# ADDITIONAL_ORIGINS=https://www.yourapp.com,https://api.yourapp.com

# Optional: Specific network host for development
# NETWORK_HOST=192.168.1.100
